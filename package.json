{
  "name": "usdt-matic",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ usdt-matic",
    "create-local": "graph create --node http://localhost:8020/ usdt-matic",
    "remove-local": "graph remove --node http://localhost:8020/ usdt-matic",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 usdt-matic"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
